VITE_API_URL=https://api.tenkasifresh.in

# Cashfree checkout environment ("production" charges real money). Not a secret —
# the App ID/Secret Key live only on the API; the browser only uses the session id.
VITE_CASHFREE_MODE=production
